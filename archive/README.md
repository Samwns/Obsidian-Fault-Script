# Archive

This folder keeps historical release-test outputs and old VSIX packages that are useful for comparison but are not part of the active build, Pages, or release workflow.

Active sources remain in `ofs/`, `ofscc/`, `packages/`, `packaging/`, `vscode-extension/`, and `website/`.
