# Archive

This folder keeps historical release-test outputs and old VSIX src/packages that are useful for comparison but are not part of the active build, Pages, or release workflow.

Active sources remain in `ofs/`, `ofscc/`, `src/packages/`, `src/packaging/`, `src/vscode-extension/`, and `src/site/`.
